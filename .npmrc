# .npmrc - Safe for both Vercel and Electron builds

# Enable shamefully-hoist for pnpm
shamefully-hoist=true

# Use strict peer dependencies
strict-peer-dependencies=false

# Logging
loglevel=warn

# This prevents electron and printer from installing on Vercel